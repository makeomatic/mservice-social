{
  "type": "object",
  "additionalProperties": false,
  "required": ["filter", "token"],
  "properties": {
    "filter": {
      "description": "What to fetch, filters are cumulative (e.g. shows feed for both account id and mentions)",
      "type": "object",
      "properties": {
        "internal": {
          "description": "Internal ID to fetch feed for",
          "type": "string"
        },
        "mentions": {
          "description": "Array of mentions to search for",
          "type": "array",
          "items": { "type": "string" }
        },
        "hashtags": {
          "description": "Array of hashtags to search for",
          "type": "array",
          "items": { "type": "string" }
        }
      }
    },
    "token": {
      "description": "Auth token",
      "type": "string"
    }
  }
}
