{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "accounts": {
      "description": "Array of feeds to sync, if not specified, syncs everything",
      "type": "array",
      "items": {
        "network": {
          "description": "Social network to fetch feed from, optional. If not specified, syncs all.",
          "type": "string"
        },
        "id": {
          "description": "Internal ID",
          "type": "string"
        }
      }
    }
  }
}
