{
  "$id": "twitter.json",
  "definitions": {
    "registerFeed": {
      "description": "Register a batch of accounts",
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["username"],
        "properties": {
          "id": {
            "description": "User identificator",
            "type": "string"
          },
          "username": {
            "description": "User name",
            "type": "string"
          }
        }
      }
    },
    "config": {
      "type": "object",
      "additionalProperties": false,
      "required": ["enabled"],
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false
        },
        "apis": {
          "$ref": "common.json#/definitions/twitterApis"
        },
        "client": {
          "$ref": "common.json#/definitions/twitterClientConfig"
        },
        "streamFilters": {
          "$ref": "common.json#/definitions/twitterStreamFilters"
        },
        "notificationPolicy": {
          "$ref": "common.json#/definitions/twitterNotificationPolicy"
        },
        "consumer_key": {
          "type": "string",
          "description": "DEPRECATED, provide config via `client` property"
        },
        "consumer_secret": {
          "type": "string",
          "description": "DEPRECATED, provide config via `client` property"
        },
        "access_token_key": {
          "type": "string",
          "description": "DEPRECATED, provide config via `client` property"
        },
        "access_token_secret": {
          "type": "string",
          "description": "DEPRECATED, provide config via `client` property"
        }
      }
    }
  }
}
